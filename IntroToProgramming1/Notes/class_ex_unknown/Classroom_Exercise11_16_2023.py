"""
Write the definition of a class Counter containing
- An instance variable counter, initialized to 0
- A method called increment that adds one to the instance variable counter.
This doesn't accept parameters or return a
value
- A method called get_value that doesn't accept any parameters.
It returns the value of the instance variable counter
"""

class Ccounter:
    def _init_(self, counter=0):
        self.counter = counter

        def increment(self): #<-- These aren't linked to anything, make sure to revisit
            self.counter += 1

        def get_value(self): #<-- These aren't linked to anything, make sure to revisit, for the sake of the assignment
            #These links to another file
            return self.counter




